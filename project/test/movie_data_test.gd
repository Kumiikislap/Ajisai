class_name MovieData extends Node

# auto completes for dialogue manager
# doesnt pass the data to functions

# for tests
var Turdus := &"Turdus"

const y_axis := 700
const left := Vector2(640 , y_axis)
const right := Vector2( 1280 , y_axis)
# expressions
var neutral := &"neutral"
var happy := &"happy"
var smile := &"smile"
var shocked := &"shocked"

func print(p_string : StringName ):
	print(p_string)

#enum {
	#Mayumi,
	#Romina,
#}

#var actors = [
	#"Mayumi",
	#"Romina",
#]

var Mayumi := &"Mayumi"
var Romina := &"Romina"
